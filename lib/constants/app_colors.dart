import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Colors.black; // Background color
  static const Color primaryTextColor =
      Color.fromRGBO(238, 238, 238, 1); // Title text color
  static const Color secondaryTextColor =
      Color.fromRGBO(238, 238, 238, 0.8); // Subtitle text color
  static const Color buttonColor =
      Color.fromRGBO(238, 238, 238, 1); // Button background color
  static const Color buttonTextColor = Color.fromRGBO(0, 0, 0, 1);
}
